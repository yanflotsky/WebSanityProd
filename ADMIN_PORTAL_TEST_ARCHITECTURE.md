# Admin Portal Test Architecture Changes

## Что было изменено

### 1. Создан новый класс `AdminPortalBaseTest`
**Путь:** `src/test/java/com/websanity/AdminPortalBaseTest.java`

**Назначение:** Базовый класс для Admin Portal тестов, который сохраняет браузер и сессию открытыми между тестами.

**Ключевые особенности:**
- Расширяет `BaseTest` (не модифицирует его)
- Использует `@TestInstance(TestInstance.Lifecycle.PER_CLASS)` для сохранения состояния между тестами
- Выполняет логин **один раз** перед всеми тестами (`@BeforeAll`)
- Закрывает контекст только после завершения всех тестов (`@AfterAll`)
- Переопределяет методы `createContextAndPage()` и `closeContext()` для предотвращения пересоздания контекста между тестами

### 2. Обновлен класс `AdminPortal`
**Изменения:**
- Теперь наследуется от `AdminPortalBaseTest` вместо `BaseTest`
- Удален метод `@BeforeEach setupAndLogin()` - логин теперь в базовом классе
- Удалены поля `loginPage` и `menuPage` - они теперь в базовом классе
- Поле `menuPage` доступно напрямую из `AdminPortalBaseTest`

### 3. BaseTest остался без изменений
**Важно:** `BaseTest` не был модифицирован, поэтому:
- ✅ `TeleadminSanityTest` продолжает работать как прежде
- ✅ Каждый его тест создает новый контекст и закрывает его после выполнения
- ✅ Нет конфликтов между разными типами тестов

## Как это работает

### Последовательность выполнения для AdminPortal тестов:

1. **@BeforeAll (BaseTest)** - запускается браузер один раз
2. **@BeforeAll (AdminPortalBaseTest)** - создается контекст, выполняется логин
3. **@Test addProUser()** - выполняется первый тест (браузер уже залогинен)
4. **@Test deleteUser()** - выполняется второй тест (браузер все еще открыт и залогинен)
5. **@AfterAll (AdminPortalBaseTest)** - закрывается контекст
6. **@AfterAll (BaseTest)** - закрывается браузер

### Преимущества:
- 🚀 **Быстрее:** логин выполняется только один раз
- 💾 **Эффективнее:** не нужно пересоздавать сессию между тестами
- 🔐 **Реалистичнее:** имитирует реальное поведение пользователя (работа в одной сессии)
- ✅ **Безопасно:** не влияет на другие тесты (TeleadminSanityTest)

## Добавление новых тестов

Чтобы добавить новый тест в Admin Portal:

```java
@Test
@Order(3)
@Story("User Management")
@Severity(SeverityLevel.CRITICAL)
@Description("Your test description")
void yourTestName() {
    // menuPage уже доступен и залогинен
    // page также доступен
    // Просто пишите логику теста
}
```

## Если нужен отдельный тип тестов с другим поведением

Создайте свой базовый класс, расширяющий `BaseTest`:

```java
public abstract class YourCustomBaseTest extends BaseTest {
    // Ваша кастомная логика
}
```

Это не повлияет на существующие тесты.

